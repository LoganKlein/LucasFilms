# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

def all_pods
  pod 'Alamofire'
  pod 'ObjectMapper'
  pod 'Kingfisher'
end

target 'LucasFilms' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  all_pods

  # Pods for LucasFilms

  target 'LucasFilmsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'LucasFilmsUITests' do
    # Pods for testing
  end

end
